# AGENTS.md - AI 开发助手指南

本文档为 AI 开发助手提供参与 `cchess-rs` 项目开发的指导。`cchess-rs` 是一个用 Rust 实现的中国象棋库。

## 项目概述

- **名称**: cchess-rs
- **描述**: 中国象棋 (Xiangqi) 的 Rust 实现
- **当前状态**: 基础结构搭建阶段，核心模块已定义但部分功能未实现
- **目标**: 完整的中国象棋规则实现，支持 AI 引擎和 UCI 协议

## 项目结构

```
cchess-rs/
├── Cargo.toml          # 项目配置
├── src/
│   ├── lib.rs          # 库入口点
│   ├── main.rs         # 可执行入口点
│   ├── board.rs        # 棋盘逻辑
│   ├── pieces.rs       # 棋子定义
│   ├── game.rs         # 游戏状态
│   └── move_gen.rs     # 走法生成
├── examples/           # 使用示例
├── tests.rs           # 单元测试
└── .gitignore         # Git 忽略文件
```

## 开发优先级

### 高优先级 (核心功能)
1. **实现棋盘初始位置** (`src/board.rs::Board::new()`)
   - 实现标准的中国象棋初始布局
   - 红方 (下方)：将、士、象、马、车、炮、兵
   - 黑方 (上方)：帅、仕、相、傌、俥、砲、卒

2. **完善走法验证** (`src/board.rs::Board::make_move()`)
   - 验证走法合法性
   - 处理吃子逻辑
   - 更新棋盘状态

3. **将军检测** (`src/game.rs::Game::check_game_over()`)
   - 检测将军状态
   - 检测将死 (绝杀)
   - 检测和棋 (长将、困毙等)

### 中优先级 (功能完善)
4. **特殊规则实现**
   - 炮的跳跃吃子规则
   - 象不能过河
   - 士不能出九宫
   - 将/帅不能照面 (飞将)

5. **走法生成优化**
   - 性能优化
   - 缓存合法走法
   - 预计算常用走法模式

### 低优先级 (扩展功能)
6. **AI 引擎基础**
   - 局面评估函数
   - 搜索算法 (alpha-beta)
   - 迭代加深

7. **UCI 协议支持**
   - UCI 命令解析
   - 最佳走法计算
   - 思考信息输出

## 开发规范

### 代码风格
- 遵循 Rust 官方代码风格
- 使用 `rustfmt` 格式化代码
- 使用 `clippy` 进行代码检查
- 添加适当的文档注释 (/// 和 //!)

### 测试要求
- 为每个新功能编写单元测试
- 测试用例应覆盖边界情况
- 使用 `#[cfg(test)]` 模块组织测试

### 错误处理
- 使用 `Result` 类型处理可恢复错误
- 使用适当的错误类型和错误信息
- 避免 panic，除非是程序无法恢复的错误

## 模块依赖关系

```
pieces.rs
    ↓
board.rs → move_gen.rs
    ↓
game.rs
    ↓
lib.rs → main.rs
```

- `pieces.rs`: 基础类型定义，无依赖
- `board.rs`: 依赖 `pieces.rs`
- `move_gen.rs`: 依赖 `board.rs` 和 `pieces.rs`
- `game.rs`: 依赖 `board.rs` 和 `pieces.rs`
- `lib.rs`: 导出所有模块
- `main.rs`: 依赖 `lib.rs`

## 常见任务模板

### 实现新功能
```rust
// 1. 在相应的模块中定义功能
// 2. 添加文档注释说明功能用途
// 3. 编写单元测试
// 4. 更新 lib.rs 中的导出（如果需要）
```

### 修复错误
```rust
// 1. 重现问题并编写测试用例
// 2. 定位问题根源
// 3. 实现修复方案
// 4. 验证修复后的测试通过
```

### 性能优化
```rust
// 1. 使用 cargo bench 进行基准测试
// 2. 使用 perf 或火焰图分析性能瓶颈
// 3. 实现优化方案
// 4. 验证优化效果并确保功能正确性
```

## 测试策略

### 单元测试位置
- 模块内部测试：放在 `#[cfg(test)]` 模块中
- 集成测试：放在 `tests/` 目录中（待创建）
- 示例测试：`examples/` 目录中的示例代码

### 测试覆盖率目标
- 核心功能：100% 覆盖率
- 边界情况：至少 90% 覆盖率
- 错误路径：覆盖主要错误情况

## 贡献指南

### 提交前检查清单
- [ ] 代码通过 `cargo check`
- [ ] 代码通过 `cargo test`
- [ ] 代码通过 `cargo clippy`
- [ ] 代码通过 `cargo fmt --check`
- [ ] 添加或更新了相关文档
- [ ] 添加了必要的测试用例

### 代码审查要点
1. **正确性**: 功能是否符合中国象棋规则
2. **安全性**: 是否避免 panic 和内存安全问题
3. **性能**: 是否有明显的性能问题
4. **可维护性**: 代码是否清晰易读
5. **测试覆盖**: 是否有足够的测试覆盖

## 已知问题和技术债务

### 当前已知问题
1. `Board::new()` 返回空棋盘，未实现初始布局
2. `Board::make_move()` 总是返回 false，未实现走法验证
3. `Game::check_game_over()` 只检查将帅是否存在，未实现完整胜负判断

### 技术债务
1. 缺少错误处理最佳实践
2. 测试覆盖率不足
3. 缺少性能基准测试
4. 文档需要完善

## 开发工具建议

### 推荐工具
- `cargo-watch`: 文件变化时自动重建
- `cargo-udeps`: 检查未使用的依赖
- `cargo-audit`: 安全漏洞检查
- `cargo-tarpaulin`: 代码覆盖率检查

### 开发命令
```bash
# 开发环境
cargo watch -x check
cargo watch -x test

# 代码质量
cargo clippy -- -D warnings
cargo fmt --check

# 性能分析
cargo bench
cargo flamegraph --bin cchess-rs

# 文档生成
cargo doc --open
```

## 中国象棋规则参考

### 棋盘布局
- 棋盘大小：9x10 格
- 红方 (下方)：坐标 (0,0) 到 (8,2)
- 黑方 (上方)：坐标 (0,7) 到 (8,9)
- 楚河汉界：第 4 和第 5 行之间

### 特殊规则
1. **将/帅**: 只能在九宫内移动，不能照面
2. **士/仕**: 只能在九宫内斜线移动
3. **象/相**: 田字移动，不能过河，有蹩腿
4. **炮/砲**: 移动如车，吃子需隔一子
5. **兵/卒**: 过河前只能前进，过河后可左右移动

## 紧急联系人/问题上报

如遇以下情况，应暂停开发并寻求指导：
- 发现严重的安全漏洞
- 遇到无法解决的编译或链接问题
- 需要修改项目架构的重大变更
- 涉及许可证或法律问题

---
*最后更新: 项目初始化阶段*
*文档维护: AI 开发助手*
